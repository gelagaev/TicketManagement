import { createAction } from "@ngrx/store";

export const showLoadingIndicator = createAction('[Common] Show Loading Indicator');
export const hideLoadingIndicator = createAction('[Common] Hide Loading Indicator');
